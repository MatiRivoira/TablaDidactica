import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matirivoira.TablaDidactica',
  appName: 'TablaDidactica',
  webDir: 'www'
};

export default config;
